name: Scrape prices

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  scrape:
    runs-on: self-hosted
    steps:
      - name: Run scraper
        shell: cmd
        working-directory: D:\Trackprice
        run: venv\Scripts\python.exe scheduler\runner.py --once
